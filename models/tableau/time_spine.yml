version: 2

models:
  - name: time_spine
    description: Time spine required for dbt Semantic Layer (MetricFlow)
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Date at daily granularity
        granularity: day
